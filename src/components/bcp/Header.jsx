import React from "react";
import './Login.css';

const Header = (props) =>{
    return(
        <header className="">
        <title>Educagood</title>
        </header>
    );
};

export default Header;